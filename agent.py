"""The agentic loop: call model -> run requested tool -> feed result back -> repeat
until the model stops or max iterations is hit. Build in Step 3 and Step 5."""

def run_agent(user_goal, max_iterations=10):
    # TODO Step 3: implement the loop with OpenAI function calling
    # TODO Step 5: add error handling + the stop condition
    raise NotImplementedError("Build the agent loop — see README.")
