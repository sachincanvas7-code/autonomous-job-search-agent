"""Streamlit UI that runs the agent and streams its reasoning. Build in Step 6."""
import streamlit as st

st.set_page_config(page_title="Job Search Agent", page_icon="🤖")
st.title("Autonomous Job Search Agent")

# TODO Step 6: upload resume, enter goal, run_agent(), stream the reasoning log
st.info("Starter scaffold — implement the steps in the README.")
